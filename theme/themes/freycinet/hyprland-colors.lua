-- Freycinet (dark) — Hyprland border colors
-- Returned as a table; hyprland.lua requires this through the theme.lua symlink.
return {
    active_border   = { colors = { "rgba(e7b174ff)", "rgba(f2d29eff)" }, angle = 45 },
    inactive_border = "rgba(263543aa)",
}

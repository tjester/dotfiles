-- Catppuccin Mocha (dark) — Hyprland border colors
-- Returned as a table; hyprland.lua requires this through the theme.lua symlink.
return {
    active_border   = { colors = { "rgba(b4befeff)", "rgba(cba6f7ff)" }, angle = 45 },
    inactive_border = "rgba(45475aaa)",
}

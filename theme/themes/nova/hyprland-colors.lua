-- Nova (dark) — Hyprland border colors
-- Returned as a table; hyprland.lua requires this through the theme.lua symlink.
return {
    active_border   = { colors = { "rgba(f8bf9bff)", "rgba(fde1a5ff)" }, angle = 45 },
    inactive_border = "rgba(3d3742aa)",
}
